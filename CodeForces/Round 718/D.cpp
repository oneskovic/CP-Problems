#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

vector<vector<vector<int>>> paths;
int n;
vector<vector<vector<int>>> weights;

void generate_of_length(int row, int col,)

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int test_cnt; cin>>test_cnt;
    for(int test_no = 0; test_no < test_cnt; test_no++)
    {
        int n,m,k;
        cin>>n>>m>>k;

        vector<vector<vector<int>>> weights(n,vector<vector<int>>(n,vector<int>()));
        for(int i = 0; i < n; i++)
        {
            for(int j = 0; j < n; j++)
            {

            }
        }
    }
    return 0;
}
